node_modules/
dist/
dist-electron/
release/
*.log
.DS_Store

# PowerShell binaries (download separately)
packages/PowerShell/

# Git portable binaries (download separately)
packages/Git/

# Electron builder caches / downloaded runtimes (large)
packages/electron-cache/
packages/electron-v*-win32-x64.zip

# Local environment (not committed)
.envrc
.env.local

# Backup files (contain tokens, passwords, and sensitive data)
better-terminal-backup-*.json
*-backup-*.json
